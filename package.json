{
  "name": "budget-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
